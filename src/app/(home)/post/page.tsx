import ThreadFeed from "@/components/thread/mobile/ThreadFeed";

export default function PostPage() {
    return <div className={"flex flex-col w-full bg-slate-50"}>
        <ThreadFeed/>
    </div>
}